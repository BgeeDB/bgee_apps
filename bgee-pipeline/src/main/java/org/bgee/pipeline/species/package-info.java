/**
 * This package provides the classes allowing to insert species and taxon information 
 * into the database. {@code GenerateTaxonOntology} allows to generate a up-to-date 
 * NCBI taxonomy ontology restricted to a defined taxonomy branch. {@code InsertTaxa} 
 * then uses this ontology to insert into Bgee species and taxonomy. 
 */
package org.bgee.pipeline.species;